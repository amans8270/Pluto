substitutions:
  _SERVICE_NAME: pluto-backend
  _REGION: us-east4
  _IMAGE: asia.gcr.io/pluto-backend-490622/pluto-backend

steps:
  # ── Step 1: Build Docker image ────────────────────────────────────────────────
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - build
      - -t
      - '${_IMAGE}:${BUILD_ID}'
      - -t
      - '${_IMAGE}:latest'
      - --cache-from
      - '${_IMAGE}:latest'
      - --build-arg
      - BUILDKIT_INLINE_CACHE=1
      - .
    dir: 'pluto-backend'

  # ── Step 2: Push to Artifact Registry ────────────────────────────────────────
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', '${_IMAGE}:${BUILD_ID}']

  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', '${_IMAGE}:latest']

  # ── Step 3: Deploy to Cloud Run ───────────────────────────────────────────────
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - run
      - services
      - replace
      - cloudrun-service.yaml
      - --region=${_REGION}
    dir: 'pluto-backend'
    env:
      - 'IMAGE=${_IMAGE}:${BUILD_ID}'

  # ── Step 4: Allow unauthenticated access ──────────────────────────────────────
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - run
      - services
      - add-iam-policy-binding
      - ${_SERVICE_NAME}
      - --region=${_REGION}
      - --member=allUsers
      - --role=roles/run.invoker

images:
  - '${_IMAGE}:${BUILD_ID}'
  - '${_IMAGE}:latest'

options:
  logging: CLOUD_LOGGING_ONLY
  machineType: E2_MEDIUM
  env:
    - 'DOCKER_BUILDKIT=1'
