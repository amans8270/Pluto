# FastAPI core
# fastapi[standard] is REQUIRED for FastAPI Cloud — it installs fastapi-cli
# which provides the 'fastapi' binary used by the platform to start the server
fastapi[standard]==0.111.1
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# Database
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.1

# Firebase Admin
firebase-admin==6.5.0

# Validation & settings
pydantic==2.7.1
pydantic-settings==2.3.0
email-validator==2.1.1

# HTTP client (for internal calls)
httpx==0.27.0

# Networking
websockets==12.0

# Auth
python-jose[cryptography]==3.3.0
PyJWT>=2.8.0
cryptography>=42.0.0

# Supabase
supabase==2.5.0

# Google Cloud Storage
google-cloud-storage==2.16.0

# Security & rate limiting
slowapi==0.1.9
python-dotenv==1.0.1

# Utilities
Pillow==10.3.0
python-dateutil==2.9.0
ujson==5.10.0
structlog==24.1.0

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7
httpx==0.27.0
